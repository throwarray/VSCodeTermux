<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Full disk access (Android 11+) so proot rootfs + user files are reachable -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Self-update / install built APKs -->
    <uses-permission 
        android:name="android.permission.REQUEST_INSTALL_PACKAGES" 
        tools:ignore="ProtectedPermissions"
    />

    <!-- code-server + proot need network (localhost loopback still counts as INTERNET) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Keep the bootstrap/code-server process alive while the IDE is open -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Termux:Boot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".VscodeTermuxApp"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.VSCodeTermux"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".WebViewActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize">
            <!--
             Android treats each matching intent-filter as a share entry. 
             Branching on intent.type/action in code instead.
            -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>

            <!--
              "Open with" support: text/* and a handful of common formats
              that get a non-text/* mimeType even though they're plain text.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" android:mimeType="text/*" />
                <data android:scheme="file" android:mimeType="text/*" />
                <data android:scheme="content" android:mimeType="application/json" />
                <data android:scheme="file" android:mimeType="application/json" />
                <data android:scheme="content" android:mimeType="application/xml" />
                <data android:scheme="file" android:mimeType="application/xml" />
                <data android:scheme="content" android:mimeType="application/javascript" />
                <data android:scheme="file" android:mimeType="application/javascript" />
                <data android:scheme="content" android:mimeType="application/x-yaml" />
                <data android:scheme="file" android:mimeType="application/x-yaml" />
                <data android:scheme="content" android:mimeType="application/x-sh" />
                <data android:scheme="file" android:mimeType="application/x-sh" />
            </intent-filter>
        </activity>
        <!--
          Same Activity, second share-sheet entry: opens a file
          with its containing folder as the workspace;
          about as close as this gets to "share a folder",
          since Android's no folder-sharing concept.
        -->
        <activity-alias
            android:name=".OpenFolderShareTarget"
            android:targetActivity=".WebViewActivity"
            android:exported="true"
            android:label="@string/share_open_folder">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity-alias>

        <activity
            android:name=".TerminalActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize"
            android:alwaysRetainTaskState="true" />

        <activity
            android:name=".ComplianceActivity"
            android:exported="true" />

        <service
            android:name=".ProotServerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Runs local dev server (code-server) inside app-managed Alpine proot" />
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
